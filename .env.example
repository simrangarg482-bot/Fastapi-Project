ENV=development

# Required in production (app raises an error at startup if missing when ENV=production)
API_KEY=demo-API_KEY
JWT_SECRET_KEY=secret

# Password for the seeded 'admin' user (see app/core/users.py)
ADMIN_PASSWORD=pass 

REDIS_URL=localhost:8000